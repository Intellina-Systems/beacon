export const SESSION_COOKIE_NAME = '_user_session_'
// Which workspace membership is active, when the signed-in account belongs to more than one.
export const ACTIVE_WORKSPACE_COOKIE_NAME = '_active_workspace_'
